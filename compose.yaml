name: inspirational-quotes

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:5173'
    volumes:
      - .:/app
      # Prevent host's node_modules from overwriting container's node_modules
      - /app/node_modules
